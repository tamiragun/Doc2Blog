futureBlog 

attrs:

ID: big serial
Topic: sting
dateToPost: string
published: bool
remPeriod: String ("D or W or M")
dateOfLastRem: String
Acknoledged bool

Methods:

checkRem() - Calls remPeriod, dateToPost, Topic dateOfLastRem, remPeriod

- checks dateOfLastRem with remPeriod agiants current date
- if time for reminder is true then check current date against due date to get numberOfDays.
- set dateOfLastRem to current date
- returns string "Blog Post (Topic) is due in (numberOfDays) on the (dateToPost)."

checkDealine()

Reminders 


